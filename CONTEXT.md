# 제품 도메인 용어집

이 문서는 국내 주식 리서치·실시간 알고리즘 신호 플랫폼의 사용자 언어와 개념 경계를 정의한다.

## 제품 경계

- **플랫폼**: 국내 개별 주식의 시장 정보, 리서치, 실시간 알고리즘 신호를 제공하는 고객용 웹 서비스.
- **고객용 웹**: 로그인하지 않은 방문자와 회원이 사용하는 웹 화면. 주문·계좌·관리자 화면은 포함하지 않는다.
- **대상 종목**: 국내 개별 주식. ETF·ETN·펀드는 대상에서 제외한다. 우선주·스팩·리츠 포함 여부는 별도 결정한다.

## 사용자·권한

- **방문자(Guest)**: 로그인하지 않은 사용자. 공개 시장·종목 정보를 볼 수 있지만 저장과 프리미엄 신호 조회는 할 수 없다.
- **회원(User)**: 회원가입 또는 간편로그인으로 생성된 사용자 계정. 관심종목을 저장할 수 있다.
- **월 구독자(Subscriber)**: 활성 월 구독을 보유한 회원. 실시간 매수·매도 알고리즘 신호와 신호 이력을 볼 수 있다.
- **구독 권한(Entitlement)**: 결제 자체가 아니라 특정 기능을 사용할 수 있는 서버 판정 결과. 구독 상태와 만료 시각을 기준으로 계산한다.

## 시장·데이터

- **시장 세션(Market session)**: 장전, 장중, 장마감, 휴장 중 하나의 시장 상태.
- **시세(Quote)**: 특정 종목의 현재가, 등락, 거래량, 체결 시각을 포함하는 실시간 데이터.
- **실시간(Real-time)**: 원천 시세 이벤트를 수신하고 허용된 지연 목표 안에서 웹과 알고리즘에 반영하는 상태. 모든 계산이 매 틱마다 공개된다는 뜻은 아니다.
- **데이터 기준 시각(asOf)**: 원천 데이터가 실제로 유효한 시각.
- **수신 시각(receivedAt)**: 플랫폼이 원천 데이터를 받은 시각.
- **발행 시각(publishedAt)**: 플랫폼이 외부 API/스트림으로 이벤트를 발행한 시각.
- **데이터 신선도(Freshness)**: 기준 시각과 현재 시각의 차이를 이용해 `REALTIME`, `DELAYED`, `STALE`, `UNAVAILABLE`을 판정하는 상태.
- **수급(Supply-demand)**: 외국인·기관·개인의 순매수와 거래량 변화 데이터.

## 분석·신호

- **AI 점수(AI score)**: 종목의 분석 결과를 비교·정렬하기 위한 수치. 현재 MVP에는 포함하지 않는 후속 후보이며, 점수가 변해도 매수·매도 신호가 반드시 변하는 것은 아니다.
- **알고리즘 신호(Signal)**: 매수 알고리즘과 매도 알고리즘이 실시간 데이터와 조건을 바탕으로 산출한 현재 상태. `매수`, `매도`, `중립`을 사용한다.
- **신호 이벤트(Signal event)**: 외부에 공개할 신호 상태 변경 기록. `eventId`와 `(streamKey, epoch, sequence)`로 멱등·순서를 관리한다.
- **신호 이력(Signal history)**: 한 종목·전략의 신호 상태 변화, 발생/발행 시각, 알고리즘 버전, 당시 evidence snapshot을 순서대로 기록한 목록.
- **신호 revision(Signal revision)**: 외부 방향 변경과 별도로 내부 재계산·입력·근거 변경을 기록하는 버전.
- **알고리즘 버전(Algorithm version)**: 동일한 신호라도 어떤 규칙·모델 버전으로 산출했는지 식별하는 값.
- **신호 상태(Signal status)**: 신호의 표시·유효성 상태로 `ACTIVE`, `SUSPENDED`, `EXPIRED`, `CANCELLED`, `VALIDATING`을 사용한다.
- **신호 신선도(Signal data status)**: 신호 계산·발행 데이터의 상태로 시세 신선도와 별도로 `REALTIME`, `DELAYED`, `STALE`, `UNAVAILABLE`을 사용한다.
- **성과(Performance)**: 신호 결과를 사후 집계한 정보. 백테스트와 실제 발생 신호의 성과는 구분하며 MVP 기본 범위가 아니다.

## 사용자 기능

- **관심종목(Watchlist)**: 사용자가 다시 확인하기 위해 저장한 종목 목록. 기존의 모호한 `내 자산`이라는 표현을 사용하지 않는다.
- **조건 스크리너(Screener)**: MVP에서는 사전 승인된 구조화 필터로 조건에 맞는 국내 개별 주식을 찾는 기능. 자연어 스크리너는 별도 승인 전 제공하지 않는다.
- **시장 인사이트(Insight)**: 시장 흐름, 업종, 수급, 뉴스와 기업 분석을 연결하는 콘텐츠.
- **구독(Subscription)**: 실시간 알고리즘 신호 접근권을 제공하는 월 단위 결제 관계.

## 상태 원칙

- 현재가의 기준 시각, 수신 시각, 화면 갱신 시각과 신호의 발생/발행 시각은 별도로 표시한다.
- 방향·신호 상태·데이터 신선도·연결·entitlement를 하나의 상태값으로 합치지 않는다.
- 권한은 화면이 아니라 서버 API와 실시간 스트림에서 최종 판정한다.
- 데이터가 오래되었거나 알고리즘 엔진이 중단된 경우 최신 신호처럼 표시하지 않는다. 엔진 장애는 watchdog 또는 조회 시 `effectiveStatus`로 판정한다.
- snapshot 이후 이벤트는 `(streamKey, epoch, sequence)` cursor로 replay/resync한다.
- `매수`·`매도` 신호는 주문 실행이나 수익 보장을 의미하지 않는다.
